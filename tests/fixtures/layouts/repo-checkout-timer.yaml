- pipeline:
    name: periodic
    manager: independent
    trigger:
      timer:
        - time: '* * * * * */1'

- job:
    name: base
    parent: null

- job:
    name: integration
    required-projects:
      - org/project1

- project:
    name: org/project1
    periodic:
      jobs:
        - integration
