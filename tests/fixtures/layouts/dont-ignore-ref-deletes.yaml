- pipeline:
    name: post
    manager: independent
    trigger:
      gerrit:
        - event: ref-updated
          ref: ^(?!refs/).*$
          ignore-deletes: false

- job:
    name: project-post
    nodes:
      - name: static
        label: ubuntu-xenial

- project:
    name: org/project
    post:
      jobs:
        - project-post
