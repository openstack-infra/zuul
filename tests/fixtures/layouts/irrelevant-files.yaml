- pipeline:
    name: check
    manager: independent
    trigger:
      gerrit:
        - event: patchset-created
    success:
      gerrit:
        Verified: 1
    failure:
      gerrit:
        Verified: -1

- job:
    name: project-test-irrelevant-files

- project:
    name: org/project
    check:
      jobs:
        - project-test-irrelevant-files:
            irrelevant-files:
              - ^README$
              - ^ignoreme$
