- pipeline:
    name: periodic
    manager: independent
    trigger:
      timer:
        - time: '* * * * * */1'

- job:
    name: project-bitrot
    nodes:
      - name: static
        label: ubuntu-xenial

- project:
    name: org/project
    periodic:
      jobs:
        - project-bitrot

