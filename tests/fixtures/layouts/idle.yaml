- pipeline:
    name: periodic
    manager: independent
    trigger:
      timer:
        - time: '* * * * * */1'

- job:
    name: base
    parent: null

- job:
    name: project-bitrot
    nodeset:
      nodes:
        - name: static
          label: ubuntu-xenial

- project:
    name: org/project
    periodic:
      jobs:
        - project-bitrot

