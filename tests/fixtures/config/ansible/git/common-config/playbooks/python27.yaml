- hosts: all
  tasks:
    - file:
        path: "{{flagpath}}"
        state: touch
    - copy:
        src: "{{zuul._test.test_root}}/{{zuul.uuid}}.flag"
        dest: "{{zuul._test.test_root}}/{{zuul.uuid}}.copied"
  roles:
    - bare-role
