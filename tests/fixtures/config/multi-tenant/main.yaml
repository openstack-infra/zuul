- tenant:
    name: tenant-one
    source:
      gerrit:
        config-repos:
          - common-config
          - tenant-one-config
        project-repos:
          - org/project1

- tenant:
    name: tenant-two
    source:
      gerrit:
        config-repos:
          - common-config
          - tenant-two-config
        project-repos:
          - org/project2
