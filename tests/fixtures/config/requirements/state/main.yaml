- tenant:
    name: tenant-one
    source:
      gerrit:
        config-repos:
          - common-config
        project-repos:
          - current-project
          - open-project
          - status-project
